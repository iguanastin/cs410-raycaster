How to compile:
    Navigate to project folder (same level as "pom.xml" file) then run

    $ mvn package


USAGE:
    $ java -jar target/cs410-2-jar-with-dependencies.jar [DRIVER_FILE]

    OPTIONS:
        DRIVER_FILE - Path to driver file

